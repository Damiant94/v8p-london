import React from 'react';
import classes from './Repairs.module.scss';

const repairs = () => {
    return (
        <section className={classes.Repairs}>
            <p>repairs</p>
        </section>
    );
};

export default repairs;